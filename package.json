{
  "name": "x-skynet",
  "private": true,
  "version": "0.1.0",
  "description": "Open-source AI agent orchestration framework — 15 minutes to your first running agent",
  "license": "Apache-2.0",
  "packageManager": "pnpm@9",
  "workspaces": [
    "packages/*",
    "examples/*",
    "templates/*"
  ],
  "scripts": {
    "build": "pnpm -r build",
    "lint": "pnpm -r lint",
    "typecheck": "pnpm -r typecheck",
    "dev": "pnpm --filter @xskynet/cli dev",
    "test": "jest",
    "test:watch": "jest --watch",
    "test:coverage": "jest --coverage"
  },
  "devDependencies": {
    "@types/jest": "^29.5.12",
    "@types/node": "^20.11.0",
    "jest": "^29.7.0",
    "ts-jest": "^29.1.2",
    "typescript": "^5.3.0"
  }
}
