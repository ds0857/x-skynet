name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

# Cancel in-progress runs on the same ref
concurrency:
  group: ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

# Minimal required permissions
permissions:
  contents: read

jobs:
  build-test:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        node-version: [20.x]
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ matrix.node-version }}
          cache: pnpm

      - name: Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 9
          run_install: false

      - name: Install dependencies (frozen lockfile)
        run: pnpm install --frozen-lockfile

      - name: Build
        run: pnpm build

      - name: Typecheck
        run: pnpm typecheck

      - name: Lint (strict)
        run: pnpm run lint -- --max-warnings 0

      - name: Test (with coverage)
        run: pnpm test

      - name: Upload lcov artifact
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: coverage-lcov-${{ matrix.node-version }}
          path: coverage/lcov.info
          if-no-files-found: warn

      # Optional: Upload coverage to Codecov using GitHub OIDC
      # To enable:
      # 1) Uncomment the step below
      # 2) Add 'id-token: write' to this job's permissions:
      #    jobs.build-test.permissions.id-token: write
      # 3) Ensure Codecov is configured (codecov.yml) or via the Codecov UI
      #
      # - name: Upload coverage to Codecov (OIDC)
      #   if: always()
      #   uses: codecov/codecov-action@v4
      #   with:
      #     use_oidc: true
      #     files: coverage/lcov.info
      #     flags: node-${{ matrix.node-version }}
      #     fail_ci_if_error: false
