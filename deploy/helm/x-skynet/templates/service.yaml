apiVersion: v1
kind: Service
metadata:
  name: {{ include "x-skynet.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "x-skynet.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "x-skynet.selectorLabels" . | nindent 4 }}
